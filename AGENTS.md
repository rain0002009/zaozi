# AGENTS.md

## Agent skills

### Issue tracker

本地 markdown 追踪（`.scratch/` 目录）。See `docs/agents/issue-tracker.md`.

### Domain docs

single-context（根目录 `CONTEXT.md` + `docs/adr/`）。See `docs/agents/domain.md`.

### Image generation

需要使用 ImageGen 生成或编辑位图素材时，操作 Herdr 中名为 `agy` 的 agent 代替完成，并保持当前项目工作目录。
