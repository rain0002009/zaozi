# ADR-0006: 距离驱动的角色动作模块

角色步态由纯 TypeScript `CharacterMotion` 模块根据实际位移、经过时间和攻击侧生成完整步态姿势，Phaser 层只负责把脚底目标通过两段腿求解应用到 rig。选择实际位移而不是按键或场景绝对时间，是为了让边界阻挡、暂停和速度变化自然停止相位；代价是场景必须在完成碰撞与边界约束后再把位移交给动作模块。

模块内部维护相位、支撑脚、落脚点、起停和方向混合，公开姿势与 rig 快照作为测试 seam。步态每 110 逻辑像素完成一个周期，起停和方向切换约 100–120ms；支撑脚世界漂移上限为 1.5 逻辑像素。
